# 서버 환경변수 예시. 실제 값은 커밋하지 않는다.
# 세션 쿠키 서명 시크릿 (운영에서는 반드시 강한 랜덤값으로 교체)
SECRET_KEY=change-me-to-a-long-random-string
# SQLite 파일 경로
DB_PATH=./data/app.db
# HTTPS 배포 시 true (Set-Cookie Secure)
COOKIE_SECURE=false
# 업로드 저장 위치 및 공개 base URL
UPLOAD_DIR=./data/uploads
UPLOAD_BASE_URL=/api/v1/uploads/files
